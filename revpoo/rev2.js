const leia = require("prompt-sync")()

let num = 0 
let contador = 0
let maiorNum = 0

do{
    total = total + numero
    if(numero>maiorNum){
        maiorNum = num
    } 
    constador++
    numero = leia("Digite um numero : ")
} while(num>0)
console.log("total : "+total)
